# red_hot
Rendering api wrapping vulkan in rust using ash for vulkan bindings

Requires the [Vulkan SDK](https://vulkan.lunarg.com/sdk/home) for both dynamic libraries and the `glslc` executable (to compile shaders in the examples).
